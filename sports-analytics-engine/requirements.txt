# Vanguard Live Quant Engine v5.0 -- Python dependencies
# Stage 1: models, config, storage.
# Stage 2: analytics (NumPy vectorized Poisson/Monte Carlo), live data
#          normalization/state (websockets provider adapter),
#          backtesting (pandas, CSV/DataFrame ingestion boundary only).

aiosqlite==0.20.0
python-dotenv==1.0.1
numpy==2.4.4
pandas==3.0.2
websockets==17.0.1

# --- test tooling ---
pytest==8.3.3
pytest-asyncio==0.24.0
